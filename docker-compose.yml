services:
  soulcast:
    build: .
    image: soulcast-iv:latest
    container_name: soulcast-iv
    ports:
      - "8000:8000"
    restart: unless-stopped
    environment:
      - PORT=8000
      - HOST=0.0.0.0